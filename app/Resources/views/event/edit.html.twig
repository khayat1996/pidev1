{% extends 'organisateur.html.twig' %}

{% block main %}
<br>
<br>
<br>
<br>
<br>
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 ml-auto mr-auto">
                <div class="card">
                    <div class="card-header card-header-primary">

                    <center>
            <h1>Event edit</h1>
            <table  class="table table-striped">
                {{ form_start(edit_form) }}
                <tr><td>Nom Organisateur :*</td><td>{{ form_widget(edit_form.nomOrg) }}</td></tr>
                <tr><td>Nom Event :* </td><td>{{ form_widget(edit_form.nomEvent) }} </td></tr>
                <tr><td>Lieu :* </td><td>{{ form_widget(edit_form.lieu) }} </td></tr>
                <tr><td>nombre de place :* </td><td>{{ form_widget(edit_form.nbPlace) }} </td></tr>
                <tr><td>Date Event :* </td><td>{{ form_widget(edit_form.dtEvent) }} </td></tr>
                <tr><td>Prix(dt) :* </td><td>{{ form_widget(edit_form.prix) }} </td><tr>
                <tr><td>Description :* </td><td>{{ form_widget(edit_form.description) }} </td><tr>
                <tr><td>Etat :* </td><td>{{ form_widget(edit_form.etat) }} </td><tr>
                <tr> <td><input type="submit" value="Enregistre" class="btn btn-outline-success"></td>

                    {{ form_end(edit_form) }}
                    <td>
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Delete" class="btn btn-outline-danger">
                        {{ form_end(delete_form) }}
                    </td>
                    <td>
                        <a class="btn btn-outline-info" href="{{ path('event_index') }}" >Back to the list</a> </td>
                    </td>
                </tr>
            </table>
        </center>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    </div>
{% endblock %}
